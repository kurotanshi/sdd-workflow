# Outcome-driven workflow research artifacts

Committed handoff notes for `sdd/outcome-driven-workflow-design`.

The executable candidate prototype, adapters, freeze file inventories, and simulated traces live outside the product package at:

`/workspace/sdd-outcome-research/`

on the research machine that produced this report. Product `skills/sdd-workflow` was not modified.

The receiving macOS machine has both CLIs authenticated, but does not have that
external directory. The [resumption preflight](resume-preflight-2026-09-18.md)
records the missing executable artifacts and approval baseline. No live pilot
runs have been collected; the recommendation remains **revise**.

See also:

- `proposal.md` / `tasks.md` under `sdd/outcome-driven-workflow-design/` (local SDD state; branch tracks those two files)
- Issue #13 for the earlier skill-slimming motivation
