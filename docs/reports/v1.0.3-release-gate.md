# v1.0.3 release gate

Date: 2026-07-29

Candidate package commit: `554c56971a081925777a2420f6a1e9129da0141c`

Release identity: `v1.0.3`

Protocol: `sdd-protocol-1.0`

Decision: PASS for publication.

The final tag may add this report after the candidate commit. The report is
outside `skills/sdd-workflow/` and does not change the validated package bytes.

## Identity

| Check | Observed |
| --- | --- |
| Engine release | `1.0.3` |
| Engine generation | `1.0` |
| CLI output | `1` |
| Discovery / handshake | `1` / `1` |
| Proposal schema range | `1..2` |
| Core protocol | `1.0.0` |
| Runtime/CLI contract | `1.0.0` |
| Agent adapter contract | `1.0.0` |

Package-local discovery, handshake, runtime identity, Skill hash, required
capabilities, CLI fixtures, compatibility matrix, READMEs, tests, and
CHANGELOG agree on the candidate identity. `runtime-identity.json` was
refreshed for the new `SKILL.md` bytes, and the refresh procedure is now
documented in CONTRIBUTING.

## Automated gates

| Gate | Result |
| --- | --- |
| Complete unit/integration suite (includes conformance, hermetic adapter, recovery drill, walkthrough, and composition coverage) | 260/260 passed |
| Package validation | PASS |
| Documentation consistency (now also validates the embedded authoring example against the current parser) | PASS |
| Trigger contract | PASS |
| macOS fresh install / release package | PASS |

Two consecutive package builds were byte-identical. The release archive
`sdd-workflow-v1.0.3.tar.gz` has SHA-256
`af6c67f7d23161229daf0574605bf4edbadc6091e2f33f1fb38501fb58fb1d0d`.

## Agent evidence

This patch changes `SKILL.md` instruction text with clarifying-only deltas
identified by the archived `prompting-best-practices-audit` research (0
conflicts, 3 tensions against the official Claude prompting best practices):

- an explicit rule that user-facing workflow output is Traditional Chinese;
- one motivation clause each on the package-local discovery, CLI fail-closed,
  and abandonment-never-reverts rules; and
- a complete worked example in `references/proposal-authoring.md`.

Natural-language triggers, explicit phase commands, report tokens, managed
mutation commands, lifecycle authority, and stop boundaries are unchanged;
the trigger contract, documentation-consistency assertions on managed
commands, and the full suite prove the unchanged surfaces. The output-language
rule codifies the convention every adherent v1.0.0 matrix run already
exercised; the historical 78-run real-host aggregate in
`evals/reports/v1.0-agent-eval-summary.md` predates this wording and is not
relabeled as v1.0.3 evidence. The CONTRIBUTING fresh-session matrix gained an
output-language row so future human acceptance checks the codified rule
explicitly.

## SemVer and scope review

The release ships instruction clarifications, contributor documentation, and
an authoring example, so it is published rather than left only on the moving
default branch. It is PATCH because the new prose states an already-exercised
convention and adds rationale and examples without changing any user
obligation, default, authority, command, schema, or machine behavior.

The release preserves engine generation `1.0`, proposal schemas v1/v2, JSON
output v1, and every machine-envelope version. It contains no fast path,
Schema v3, locking or lease system, Web UI, external-platform authority, or
multi-Agent orchestration.

The staged release contains no raw Agent transcripts, secret-bearing eval
artifacts, absolute user paths, or unrelated working-tree changes.
