# v1.0.1 release gate

Date: 2026-07-27

Candidate package commit: `a8d767b56d6750ceb8a9a82aff5d7e5bb8352596`

Release identity: `v1.0.1`

Protocol: `sdd-protocol-1.0`

Decision: PASS for publication.

The final tag may add this report after the candidate commit. The report is
outside `skills/sdd-workflow/` and does not change the validated package bytes.

## Identity

| Check | Observed |
| --- | --- |
| Engine release | `1.0.1` |
| Engine generation | `1.0` |
| CLI output | `1` |
| Discovery / handshake | `1` / `1` |
| Proposal schema range | `1..2` |
| Core protocol | `1.0.0` |
| Runtime/CLI contract | `1.0.0` |
| Agent adapter contract | `1.0.0` |

Package-local discovery, handshake, runtime identity, Skill hash, required
capabilities, CLI fixtures, compatibility matrix, READMEs, tests, and release
notes agree on the candidate identity.

## Automated gates

| Gate | Result |
| --- | --- |
| Complete unit/integration suite | 259/259 passed |
| Reference-runtime conformance | 18/18 cases passed |
| Public conformance kit | 5/5 cases passed |
| Hermetic Agent adapter | 10/10 scenarios passed |
| Recovery drill groups | 10/10 passed; 51 selected tests |
| Package validation | PASS |
| Documentation consistency | PASS |
| Trigger contract | PASS |
| macOS fresh install / release package | PASS |
| Isolated full lifecycle and uninstall/no residue | PASS |
| Sample web API repository walkthrough | PASS |
| Security-review composition example | PASS |
| Public eval report safety/review contract | PASS |

Two consecutive package builds were byte-identical. The release archive
`sdd-workflow-v1.0.1.tar.gz` has SHA-256
`db4dafca34d00bb801f1033aa9a670ccea627f6fdf965ff40d9fcabcc1fa0bd2`.

## Agent evidence

This patch does not change `SKILL.md`, trigger rules, or Agent orchestration.
The complete v1.0.0 real-host matrix is therefore reused from exact Skill
commit `476afa25371ec7c8cbaa8859ce760b303cec02cd`:

- 78/78 valid planned runs;
- 76/78 adherent (97.4%) against the 95% gate;
- zero Critical Violations;
- zero invalid attempts; and
- 6/6 adherent runs for acceptance-time requirement change.

The authorized-revision and interrupted-reapproval fixes at commit
`fe04aa12fdccc73b45032d43ee17d088f891952b` add fresh focused evidence across
Codex and Claude scenarios B, D, J, H, and M: 10 valid runs, zero Critical
Violations, with M passing the final oracle on both hosts.

The legacy mutation-safety fix changes only deterministic runtime compatibility
handling: readable legacy proposals remain readable, while five mutation
commands now return the stable fail-closed error and perform no writes. Focused
CLI regressions cover every affected command, JSON and text envelopes,
read-only continuity, filesystem immutability, and current-schema behavior.
Runtime conformance and recovery gates were rerun on the candidate.

Full historical aggregate details and publication review remain in
`evals/reports/v1.0-agent-eval-summary.md`; this report does not relabel those
executions as v1.0.1 runs.

## Recovery and examples

Recovery evidence covers safe retry, atomic writes, partial metadata,
committed archive/index recovery, concurrent convergence, same-snapshot stale
rejection, unknown compatibility, future schema, and external proposal moves.

The sample repository replayed proposal, approval, task implementation,
approval-relevant drift, managed revision/reapproval, acceptance, archive,
derived INDEX deletion, rebuild, and healthy doctor state. The composition
example completed an upper-level security review using standard SDD primitives
without extending protocol state.

## Scope and publication review

The release preserves engine generation `1.0`, proposal schemas v1/v2, JSON
output v1, and every machine-envelope version. It contains no Schema v3,
locking or lease system, Web UI, external-platform authority, or multi-Agent
orchestration.

The staged release contains no raw Agent transcripts, secret-bearing eval
artifacts, absolute user paths, or unrelated working-tree changes.
