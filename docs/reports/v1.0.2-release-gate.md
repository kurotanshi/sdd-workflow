# v1.0.2 release gate

Date: 2026-07-27

Candidate package commit: `c25bb46fae7c81e0734812bc4cefd465f8836e78`

Release identity: `v1.0.2`

Protocol: `sdd-protocol-1.0`

Decision: PASS for publication.

The final tag may add this report after the candidate commit. The report is
outside `skills/sdd-workflow/` and does not change the validated package bytes.

## Identity

| Check | Observed |
| --- | --- |
| Engine release | `1.0.2` |
| Engine generation | `1.0` |
| CLI output | `1` |
| Discovery / handshake | `1` / `1` |
| Proposal schema range | `1..2` |
| Core protocol | `1.0.0` |
| Runtime/CLI contract | `1.0.0` |
| Agent adapter contract | `1.0.0` |

Package-local discovery, handshake, runtime identity, Skill hash, required
capabilities, CLI fixtures, compatibility matrix, READMEs, tests, and
CHANGELOG agree on the candidate identity.

## Automated gates

| Gate | Result |
| --- | --- |
| Complete unit/integration suite | 260/260 passed |
| Reference-runtime conformance | 18/18 cases passed |
| Public conformance kit | 5/5 cases passed |
| Hermetic Agent adapter | 10/10 scenarios passed |
| Recovery drill groups | 10/10 passed; 51 selected tests |
| Package validation | PASS |
| Documentation consistency | PASS |
| Trigger contract | PASS |
| macOS fresh install / release package | PASS |
| Isolated full lifecycle and uninstall/no residue | PASS |
| Sample web API repository walkthrough | PASS |
| Security-review composition example | PASS |
| Public eval report safety/review contract | PASS |

Two consecutive package builds were byte-identical. The release archive
`sdd-workflow-v1.0.2.tar.gz` has SHA-256
`60561479d3f9e341a2760bc680a34f803ab6fdd89d8df8fe4353a034b3009aa9`.

## Agent evidence

This patch changes README positioning and the engine release identity only. It
does not change `SKILL.md`, natural-language triggers, explicit phase commands,
Agent orchestration, lifecycle authority, or stop boundaries. No Agent
interaction boundary is affected.

The complete v1.0.0 real-host matrix is therefore reused from exact Skill
commit `476afa25371ec7c8cbaa8859ce760b303cec02cd`:

- 78/78 valid planned runs;
- 76/78 adherent (97.4%) against the 95% gate;
- zero Critical Violations;
- zero invalid attempts; and
- 6/6 adherent runs for acceptance-time requirement change.

The v1.0.2 candidate adds deterministic bilingual README assertions for the
broader task positioning, concise small-task proposal guidance, preserved
safety boundaries, and Agent-driven use of the bundled Python runtime. The
unchanged trigger contract and all 10 hermetic adapter scenarios passed.

Full historical aggregate details and publication review remain in
`evals/reports/v1.0-agent-eval-summary.md`; this report does not relabel those
executions as v1.0.2 runs.

## SemVer and scope review

The release changes published usage guidance, so it is shipped rather than
left only on the moving default branch. It is PATCH because the new prose
clarifies the intended applicability of the existing workflow without changing
any user obligation, default, authority, command, schema, or machine behavior.

The release preserves engine generation `1.0`, proposal schemas v1/v2, JSON
output v1, and every machine-envelope version. It contains no fast path,
Schema v3, locking or lease system, Web UI, external-platform authority, or
multi-Agent orchestration.

The staged release contains no raw Agent transcripts, secret-bearing eval
artifacts, absolute user paths, or unrelated working-tree changes.
